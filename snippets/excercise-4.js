/********** EXCERCISE 4 **********/
var arr = [3, 2, 1];
arr.sort();
// what is the value of arr after sort()?
arr.concat(4);
// what is the value of arr after concat()?
arr.slice(0);
// what is the value of arr after slice()?
arr.splice(0);
// what is the value of arr after splice()?
